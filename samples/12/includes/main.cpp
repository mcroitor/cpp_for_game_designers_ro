#include "mymath.h"
#include "mymath.h"
//#include <cstdio>

int main() {
    int a = -5;
    int b = 10;
    int c = myabs(a);
    int d = mymin(a, b);

    //printf("Absolute value of %d is %d\n", a, c);
    //printf("Minimum of %d and %d is %d\n", a, b, d);
    return 0;
}